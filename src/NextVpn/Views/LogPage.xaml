<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="NextVpn.Views.LogPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vm="using:NextVpn.ViewModels"
    mc:Ignorable="d"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006">

    <Grid Padding="{StaticResource PagePadding}" RowSpacing="12" MaxWidth="1180">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="Activity" Style="{StaticResource PageTitle}" />
            <TextBlock Style="{StaticResource Secondary}"
                       Text="Live output from the tunnel engine. Useful when a network is blocking connections." />
        </StackPanel>

        <Grid Grid.Row="1" ColumnSpacing="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <ToggleSwitch Grid.Column="0" MinWidth="0"
                          OnContent="Verbose diagnostics" OffContent="Verbose diagnostics"
                          IsOn="{x:Bind ViewModel.ShowDiagnostics, Mode=TwoWay}" />

            <Button Grid.Column="2" Click="OnCopy">
                <StackPanel Orientation="Horizontal" Spacing="7">
                    <FontIcon Glyph="&#xE8C8;" FontSize="14" />
                    <TextBlock Text="Copy all" />
                </StackPanel>
            </Button>
            <Button Grid.Column="3" Command="{x:Bind ViewModel.ClearLogCommand}">
                <StackPanel Orientation="Horizontal" Spacing="7">
                    <FontIcon Glyph="&#xE74D;" FontSize="14" />
                    <TextBlock Text="Clear" />
                </StackPanel>
            </Button>
        </Grid>

        <Border Grid.Row="2" Style="{StaticResource Card}" Padding="4">
          <Grid>
            <StackPanel x:Name="EmptyNote" Spacing="6"
                        HorizontalAlignment="Center" VerticalAlignment="Center" MaxWidth="360">
                <FontIcon Glyph="&#xE9D9;" FontSize="22" HorizontalAlignment="Center"
                          Foreground="{ThemeResource TextFillColorTertiaryBrush}" />
                <TextBlock Style="{StaticResource Secondary}" TextAlignment="Center"
                           Text="Nothing yet. Everything the tunnel engine reports appears here as it happens." />
            </StackPanel>

            <ListView x:Name="LogList"
                      ItemsSource="{x:Bind ViewModel.Log, Mode=OneWay}"
                      SelectionMode="None"
                      ScrollViewer.VerticalScrollBarVisibility="Hidden"
                      ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="Padding" Value="8,3" />
                        <Setter Property="MinHeight" Value="0" />
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    </Style>
                </ListView.ItemContainerStyle>

                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="vm:LogEntry">
                        <Grid ColumnSpacing="10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" Text="{x:Bind TimeText}"
                                       FontFamily="Consolas" FontSize="11"
                                       Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                                       VerticalAlignment="Top" />

                            <TextBlock Grid.Column="1" Text="{x:Bind Type}"
                                       FontSize="11" FontWeight="SemiBold" MinWidth="132"
                                       Foreground="{x:Bind Level, Converter={StaticResource LogLevelToBrush}}"
                                       VerticalAlignment="Top" TextTrimming="CharacterEllipsis" />

                            <TextBlock Grid.Column="2" Text="{x:Bind Message}"
                                       Style="{StaticResource CaptionTextBlockStyle}"
                                       TextWrapping="Wrap" IsTextSelectionEnabled="True" />
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
          </Grid>
        </Border>
    </Grid>
</Page>
